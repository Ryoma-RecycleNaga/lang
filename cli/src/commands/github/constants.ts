
// paths
export const GH_SLUG = `bazar`;
export const CONFIG_FILE = 'config.json';
export const VENDOR_CONFIG = `${GH_SLUG}/${CONFIG_FILE}`;
export const PRODUCT_CONFIG = (product_path) => `${product_path}/${CONFIG_FILE}`;

// template keys
export const INDEX_TEMPLATE = 'product_index.md';

