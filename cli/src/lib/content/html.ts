export const img = (file) => {
    return `<div class="thumb">
            <a href="${file}" _target="_blank" >
                <img src="${file}" width="100%" />
            </a>
        </div>`;
}
